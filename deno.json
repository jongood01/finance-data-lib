{
  "lint": {
    "rules": {
      "exclude": ["no-explicit-any"]
    }
  },
  "tasks": {
    "tag": "git tag Deno.env.get('VERSION') && git push origin Deno.env.get('VERSION')",
    "build": "deno run -A scripts/build_npm.ts Deno.env.get('VERSION')",
    "deploy": "cd npm && npm publish",
    "build:deploy": "deno task tag && deno task build && deno task deploy",
    "cache": "deno cache mod.ts"
  },
  "importMap": "import-map.json"
}
